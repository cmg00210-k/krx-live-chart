% CheeseStock ANATOMY V8 -- Theoretical Coherence Flow XeLaTeX Template
% Academic lineage: Data → Theory → TA → Chart → Website
% Dense professional layout with stage navigator
\documentclass[10pt, a4paper, oneside]{article}

% ============================================================
% 1. PAGE GEOMETRY -- Bloomberg-style margins
% ============================================================
\usepackage[
  a4paper,
  top=20mm,
  bottom=20mm,
  left=22mm,
  right=22mm,
  headsep=12mm,
  footskip=12mm
]{geometry}

% ============================================================
% 2. FONTS -- CJK + Latin + Monospace
% ============================================================
\usepackage{fontspec}
\usepackage{xeCJK}

\setmainfont{Calibri}[Numbers={Lining,Monospaced}]
\setsansfont{Calibri}[Numbers={Lining,Monospaced}]
\setmonofont{Consolas}[Scale=0.92]

% Korean font: Pretendard (static OTF, 9 weights installed in C:/Windows/Fonts/)
% V21 note: V20 attempted Noto Sans KR VF variable font with wght axis (400/700)
% but TeXLive 2024 fontspec only embedded the default Thin instance, losing
% Regular/Bold hierarchy. V21 switches to Pretendard static OTF files with
% explicit file-name specification (NOT variable axis, NOT family-name lookup).
\setCJKmainfont{Pretendard-Regular.otf}[
  Path = C:/Windows/Fonts/,
  BoldFont = Pretendard-Bold.otf,
  ItalicFont = Pretendard-Regular.otf,
  BoldItalicFont = Pretendard-Bold.otf
]
\setCJKsansfont{Pretendard-Regular.otf}[
  Path = C:/Windows/Fonts/,
  BoldFont = Pretendard-Bold.otf
]
\setCJKmonofont{Pretendard-Regular.otf}[
  Path = C:/Windows/Fonts/,
  BoldFont = Pretendard-Bold.otf,
  Scale = 0.88
]

\xeCJKsetup{
  CJKspace=true,
  CJKecglue={\hskip 0.12em plus 0.06em minus 0.04em}
}

% Korean-specific linebreak rules (XeTeX ICU locale)
% Without this, XeTeX falls back to CJK default (Chinese) rules,
% ignoring Korean 어절 boundaries and causing unnatural wraps.
\XeTeXlinebreaklocale "ko"
\XeTeXlinebreakskip = 0pt plus 1pt minus 0pt

% ============================================================
% 3. COLORS -- 5-Stage system + common
% ============================================================
\usepackage[dvipsnames,svgnames,x11names]{xcolor}

% Stage 1: Data & API -- Slate Blue
\definecolor{stageOne}{HTML}{2C3E5C}
\definecolor{stageOneMarker}{HTML}{4A6FA5}
\definecolor{stageOneCodeBg}{HTML}{F2F5FA}
\definecolor{stageOneQuoteBg}{HTML}{EBF0F7}

% Stage 2: Academic Foundations -- Amber Gold
\definecolor{stageTwo}{HTML}{3D3000}
\definecolor{stageTwoMarker}{HTML}{A08830}
\definecolor{stageTwoCodeBg}{HTML}{FDFAF0}
\definecolor{stageTwoQuoteBg}{HTML}{F2EAC8}

% Stage 3: Technical Analysis -- Emerald Teal
\definecolor{stageThree}{HTML}{1A3D35}
\definecolor{stageThreeMarker}{HTML}{2A7A6A}
\definecolor{stageThreeCodeBg}{HTML}{F0FAF7}
\definecolor{stageThreeQuoteBg}{HTML}{DFF0E8}

% Stage 4: Chart Visualization -- Deep Violet
\definecolor{stageFour}{HTML}{2D1B4E}
\definecolor{stageFourMarker}{HTML}{6A4FA5}
\definecolor{stageFourCodeBg}{HTML}{F5F0FA}
\definecolor{stageFourQuoteBg}{HTML}{E5DAEF}

% Stage 5: Website Delivery -- Warm Espresso
\definecolor{stageFive}{HTML}{3A2010}
\definecolor{stageFiveMarker}{HTML}{8B5A2B}
\definecolor{stageFiveCodeBg}{HTML}{FAF5EF}
\definecolor{stageFiveQuoteBg}{HTML}{EEE0D0}

% Common colors
\definecolor{navy}{HTML}{2C3E6B}
\definecolor{dark}{HTML}{1A1A1A}
\definecolor{bodytext}{HTML}{1A1A1A}
\definecolor{muted}{HTML}{555555}
\definecolor{tableodd}{HTML}{F7F7F7}

% Dynamic Stage color aliases -- switched by Lua filter on each Stage entry
\colorlet{curStageMarker}{stageOneMarker}
\colorlet{curStageCodeBg}{stageOneCodeBg}
\colorlet{curStageQuoteBg}{stageOneQuoteBg}
\colorlet{curStageDark}{stageOne}

% ============================================================
% 3b. MATH PACKAGES -- required for D3 theoretical foundations (amsmath text, symbols)
% ============================================================
\usepackage{amsmath}
\usepackage{amssymb}

% ============================================================
% 3c. FOOTNOTES -- academic reference style (핵심문헌 각주)
% ============================================================
\usepackage[hang,flushmargin,bottom]{footmisc}
\setlength{\jot}{4pt}  % amsmath aligned/cases internal row gap (default 3pt)

% Display math spacing (Bringhurst: body size × 0.8~1.0)
\AtBeginDocument{%
  \abovedisplayskip=8pt plus 3pt minus 3pt
  \belowdisplayskip=8pt plus 3pt minus 3pt
  \abovedisplayshortskip=4pt plus 2pt minus 2pt
  \belowdisplayshortskip=4pt plus 2pt minus 2pt
}

% ============================================================
% 4. PARAGRAPH SPACING
% ============================================================
\usepackage{parskip}
\setlength{\parindent}{0pt}
\setlength{\parskip}{3pt plus 1pt minus 1pt}

\usepackage{setspace}
\setstretch{1.35}

% ============================================================
% 5. HEADING STYLES -- clear visual hierarchy
% ============================================================
\usepackage{titlesec}
\usepackage{needspace}
\usepackage[defaultlines=3,all]{nowidow}

% H1 (Stage titles): handled by Lua filter with navigator
% Fallback for non-Stage H1 (appendices etc)
\titleformat{\section}
  {\needspace{4\baselineskip}\Large\bfseries\sffamily\color{dark}}
  {}{0pt}{}
\titlespacing*{\section}{0pt}{14pt}{4pt}[0pt]

% H2: Stage-colored, clear separation from H1
% needspace 12 = ~162pt (23% of page) -- pushes headings at 85%+ to next page
% 12pt (not 13pt): widens gap from H1(14.3pt) -- ratio 1.19x vs 1.10x
\titleformat{\subsection}
  {\needspace{12\baselineskip}\fontsize{12pt}{15pt}\selectfont\bfseries\sffamily\color{curStageMarker}}
  {}{0pt}{}
\titlespacing*{\subsection}{0pt}{10pt}{3pt}[0pt]

% H3: Stage-tinted, subordinate -- 10pt matches body size, color differentiates
% needspace 10 = ~135pt (20% of page) -- pushes headings at 85%+ to next page
% (increased from 8 so concept-introducing H3s naturally push to next page near bottom)
\titleformat{\subsubsection}
  {\needspace{10\baselineskip}\fontsize{10pt}{12.5pt}\selectfont\bfseries\sffamily\color{curStageMarker!65!black}}
  {}{0pt}{}
\titlespacing*{\subsubsection}{0pt}{10pt}{2pt}[0pt]

% H4: inline, small -- needspace prevents orphan at very bottom
\titleformat{\paragraph}[runin]
  {\needspace{3\baselineskip}\small\bfseries\sffamily\color{muted}}
  {}{0pt}{}[.]
\titlespacing*{\paragraph}{0pt}{4pt}{4pt}

% ============================================================
% 6. TABLES
% ============================================================
\usepackage{calc}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{array}
\usepackage{multirow}
\usepackage{colortbl}
\usepackage{caption}

\usepackage{adjustbox}
\renewcommand{\arraystretch}{1.20}          % Row height: 1.20 tighter than 1.35 -- more rows per page
\setlength{\tabcolsep}{2.5pt}              % 2.5pt (was 3.5pt) -- ~1.4mm more content per 5-col table
\setlength{\emergencystretch}{1.5em}       % 1.5em (was 3em) -- prevents excessive stretch on loose lines
\tolerance=2000                             % Allow loose boxes up to badness 2000 (default 200)
\pretolerance=500                           % First-pass tolerance before hyphenation attempts
% extrarowheight removed: it adds padding only on top, creating asymmetric CJK cells.
% arraystretch=1.35 provides symmetric spacing instead.
\setlength{\aboverulesep}{2pt}             % booktabs: slightly more breathing above rules
\setlength{\belowrulesep}{2pt}             % booktabs: slightly more breathing below rules
\setlength{\LTpost}{6pt}                   % longtable post-spacing (6pt for table-sourcecite gap)

% Booktabs + hline rules: Stage-colored at 55% for screen/print visibility
\arrayrulecolor{curStageMarker!55}
\captionsetup{font=small, labelfont=bf, skip=4pt}
% Font sizing is delegated entirely to the Lua Table() filter.
% AtBeginEnvironment hooks only enforce sloppy breaking.
\AtBeginEnvironment{longtable}{\sloppy\setlength{\emergencystretch}{1.5em}\hyphenpenalty=200\exhyphenpenalty=200}
\AtBeginEnvironment{tabular}{\sloppy\setlength{\emergencystretch}{1.5em}\hyphenpenalty=200\exhyphenpenalty=200}

% ============================================================
% 7. CODE BLOCKS
% ============================================================
\usepackage{fancyvrb}
\usepackage{framed}

\DefineVerbatimEnvironment{Highlighting}{Verbatim}{
  fontsize=\fontsize{8.5pt}{11pt}\selectfont,
  xleftmargin=0pt,
  xrightmargin=0pt,
  commandchars=\\\{\}
}

\usepackage{etoolbox}
\makeatletter
\preto{\@verbatim}{\topsep=4pt \partopsep=0pt}
\makeatother

\newenvironment{Shaded}{%
  \begin{snugshade}%
}{%
  \end{snugshade}%
}
\definecolor{shadecolor}{HTML}{F4F4F6}

% ============================================================
% 8. BLOCKQUOTES -- Stage-colored left bar + light background
% ============================================================
\usepackage{mdframed}

\renewenvironment{quote}{%
  \begin{mdframed}[
    linewidth=2.5pt,
    linecolor=curStageMarker,
    leftline=true, topline=false, rightline=false, bottomline=false,
    backgroundcolor=curStageQuoteBg,
    innerleftmargin=8pt,
    innerrightmargin=8pt,
    innertopmargin=4pt,
    innerbottommargin=4pt,
    skipabove=4pt,
    skipbelow=4pt
  ]\small\sffamily
}{%
  \end{mdframed}%
}

% ============================================================
% 8b. SOURCE CITATIONS -- small, muted, right-aligned
% ============================================================
% sourcecite: \hfill avoids trivlist topsep; 3pt lead prevents flush-to-table
\newenvironment{sourcecite}{%
  \par\vspace{3pt}\noindent\hfill%
  \fontsize{7.5pt}{9.5pt}\selectfont\sffamily\color{muted}\itshape%
}{%
  \par\vspace{2pt}%
}

% ============================================================
% 8c. STAGE META -- light gray box for Stage header metadata
% ============================================================
\newenvironment{stagemeta}{%
  \vspace{2pt}%
  \begin{mdframed}[
    linewidth=0pt,
    backgroundcolor=tableodd,
    innerleftmargin=8pt,
    innerrightmargin=8pt,
    innertopmargin=3pt,
    innerbottommargin=3pt,
    skipabove=2pt,
    skipbelow=4pt
  ]\fontsize{7pt}{9pt}\selectfont\sffamily\color{muted}\sloppy%
}{%
  \end{mdframed}%
}

% ============================================================
% 8d. KEYPOINT BOX -- non-expert reader summary (expanded)
% ============================================================
\newenvironment{keypoint}[1]{%
  \vspace{6pt}%
  \begin{mdframed}[
    linewidth=1.5pt,
    linecolor=curStageMarker!60,
    topline=true, bottomline=true, leftline=true, rightline=false,
    backgroundcolor=white,
    innerleftmargin=12pt,
    innerrightmargin=12pt,
    innertopmargin=8pt,
    innerbottommargin=8pt,
    skipabove=6pt,
    skipbelow=6pt
  ]%
  % Header: 9pt so it sits at same scale as body (9.5pt), not smaller
  \noindent{\fontsize{9pt}{12pt}\selectfont\sffamily\bfseries\color{curStageMarker!80!black}%
    \textcolor{curStageMarker}{\raisebox{1pt}{\rule{5pt}{5pt}}}\enspace #1}\par\vspace{4pt}%
  \fontsize{9.5pt}{13pt}\selectfont\sffamily\color{bodytext}%
}{%
  \end{mdframed}%
  \vspace{6pt}%
}

% ============================================================
% 8e. FOOTNOTES -- academic reference style
% ============================================================
\renewcommand{\footnotelayout}{\fontsize{7.5pt}{9.5pt}\selectfont}
\renewcommand{\footnoterule}{%
  \kern-3pt
  {\color{curStageMarker!40}\hrule width 0.4\textwidth height 0.4pt}
  \kern 2.6pt
}
\setlength{\footnotesep}{6pt}

% ============================================================
% 9. LISTS -- compact
% ============================================================
\usepackage{enumitem}
\setlist{nosep, leftmargin=1.5em, topsep=2pt}
\setlist[itemize]{label=\textbullet}

% ============================================================
% 10. HEADERS / FOOTERS -- Stage-colored accent rule
% ============================================================
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\fontsize{8pt}{10pt}\selectfont\sffamily\color{muted}%
  CheeseStock ANATOMY V8%
  {\color{curStageMarker!60}~|~}%
  이론적 정합성 흐름}
% Right header: show current Stage (set by Lua filter), NOT subsection name.
% Subsection \rightmark was duplicating the heading text below it.
\newcommand{\currentstage}{}
\fancyhead[R]{\fontsize{8pt}{10pt}\selectfont\sffamily\color{muted}%
  \currentstage}
\fancyfoot[C]{\small\sffamily\thepage}
\renewcommand{\headrulewidth}{0.3pt}
\renewcommand{\headrule}{{\color{curStageMarker!40}\hrule width\headwidth height\headrulewidth}}
% Prevent subsection from setting \rightmark (avoids header/heading duplication)
\renewcommand{\subsectionmark}[1]{}
\renewcommand{\subsubsectionmark}[1]{}

\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{\small\sffamily\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% ============================================================
% 11. HYPERLINKS
% ============================================================
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=navy,
  urlcolor=stageOneMarker,
  citecolor=navy,
  pdfauthor={이선호, 최민규},
  pdftitle={CheeseStock ANATOMY V8 이론적 정합성 흐름},
  pdfsubject={KRX 기술적 분석의 학술적 계보},
  bookmarksnumbered=true,
  bookmarksopen=true,
  bookmarksopenlevel=2
}
\usepackage{bookmark}

% ============================================================
% 12. MISC
% ============================================================
\usepackage{graphicx}
\usepackage{float}
\usepackage{pdflscape}
\usepackage{microtype}

\setlength{\intextsep}{6pt}
\setlength{\abovecaptionskip}{4pt}
\setlength{\belowcaptionskip}{2pt}

\widowpenalty=10000
\clubpenalty=10000

% Suppress page breaks immediately after headings (keep heading with content)
\makeatletter
\renewcommand\@afterheading{%
  \@nobreaktrue
  \everypar{%
    \if@nobreak
      \@nobreakfalse
      \clubpenalty \@M  % 10000 = effectively infinite penalty
    \else
      \clubpenalty \@clubpenalty
      \everypar{}%
    \fi}}
\makeatother

% ============================================================
% 13. STAGE NAVIGATOR
% ============================================================
% Compact horizontal 5-box strip showing current position
\newcommand{\stagenavigator}[1]{%
  \vspace{4pt}%
  \noindent%
  \begin{tabularx}{\textwidth}{@{}XXXXX@{}}
    \stagenavbox{1}{데이터·API}{stageOneMarker}{#1} &
    \stagenavbox{2}{학술 기반}{stageTwoMarker}{#1} &
    \stagenavbox{3}{기술적 분석}{stageThreeMarker}{#1} &
    \stagenavbox{4}{차트}{stageFourMarker}{#1} &
    \stagenavbox{5}{최종 전달}{stageFiveMarker}{#1}
  \end{tabularx}%
  \vspace{6pt}\par%
}

\newcommand{\stagenavbox}[4]{%
  % #1=number, #2=label, #3=color, #4=current stage number
  \ifnum#1=#4\relax%
    \colorbox{#3}{%
      \parbox[c][10mm]{\dimexpr\linewidth-2\fboxsep\relax}{\centering\color{white}%
        \fontsize{7.5pt}{9pt}\selectfont\sffamily\bfseries Stage #1\\[-1pt]%
        \fontsize{6.5pt}{8pt}\selectfont #2%
      }%
    }%
  \else%
    \colorbox{tableodd}{%
      \parbox[c][10mm]{\dimexpr\linewidth-2\fboxsep\relax}{\centering\color{muted}%
        \fontsize{7.5pt}{9pt}\selectfont\sffamily Stage #1\\[-1pt]%
        \fontsize{6.5pt}{8pt}\selectfont #2%
      }%
    }%
  \fi%
}

% ============================================================
% 13b. STAGE OVERVIEW PAGE -- 5-color horizontal flow (replaces redundant title on page 3)
% ============================================================
% Single card macro: fixed-height minipage enforces baseline alignment.
% Colored top banner + title + subtitle + description with uniform vertical structure.
\newcommand{\stagecard}[5]{%
  % #1=number, #2=color, #3=title, #4=subtitle, #5=description
  \begin{minipage}[t][32mm][t]{\linewidth}%
    \colorbox{#2}{%
      \parbox[c][9mm]{\dimexpr\linewidth-2\fboxsep\relax}{\centering\color{white}%
        \fontsize{8.5pt}{10pt}\selectfont\sffamily\bfseries 제#1장%
      }%
    }\\[3pt]%
    {\centering\fontsize{7.5pt}{9.5pt}\selectfont\sffamily%
    \textbf{\color{#2}#3}\par}%
    \vspace{1pt}%
    {\centering\fontsize{6.5pt}{8pt}\selectfont\color{muted}%
    #4\par}%
    \vspace{3pt}%
    {\raggedright\fontsize{6.5pt}{9pt}\selectfont\color{bodytext}%
    \XeTeXlinebreaklocale "ko"\XeTeXlinebreakskip=0pt plus 1pt%
    #5\par}%
  \end{minipage}%
}
% Arrow between cards: thin colored arrow, vertically centered at card top-band height
\newcommand{\stageflowsep}{%
  \raisebox{24mm}{\fontsize{10pt}{10pt}\selectfont\color{muted!55}\textrightarrow}%
}
\newcommand{\stageoverviewflow}{%
  \vspace{8pt}%
  \noindent{\large\bfseries\sffamily\color{navy} 5단계 이론적 정합성 흐름}%
  \vspace{10pt}\par%
  \noindent%
  \begin{tabularx}{\textwidth}{@{}X@{\hspace{3pt}}c@{\hspace{3pt}}X@{\hspace{3pt}}c@{\hspace{3pt}}X@{\hspace{3pt}}c@{\hspace{3pt}}X@{\hspace{3pt}}c@{\hspace{3pt}}X@{}}
      \stagecard{1}{stageOneMarker}{데이터와 API}{원천 자료}{8개 API, OHLCV + 거시경제 + 파생상품} &
    \stageflowsep &
      \stagecard{2}{stageTwoMarker}{학술적 기반}{이론적 토대}{47개 문서, 6개 학문 분야, 200건 이상 인용} &
    \stageflowsep &
      \stagecard{3}{stageThreeMarker}{기술적 분석}{이론의 적용}{25개 이상 지표, 31개 패턴, IC 계보} &
    \stageflowsep &
      \stagecard{4}{stageFourMarker}{차트}{시각적 변환}{9개 드로우 레이어, 색채 이론, 밀도 제어} &
    \stageflowsep &
      \stagecard{5}{stageFiveMarker}{최종 전달}{cheesestock.co.kr}{4열 그리드, 5개 종단간 추적} \\
  \end{tabularx}%
  \vspace{8pt}\par%
}

% ============================================================
% 13a. TOC SPACING -- compact single-page TOC
% ============================================================
\usepackage{tocloft}
\setlength{\cftbeforesecskip}{5pt}
\setlength{\cftbeforesubsecskip}{0.5pt}
\setlength{\cftbeforetoctitleskip}{0pt}
\setlength{\cftaftertoctitleskip}{8pt}
\renewcommand{\cftsecfont}{\sffamily\bfseries\color{dark}}
\renewcommand{\cftsecpagefont}{\sffamily\bfseries\color{dark}}
\renewcommand{\cftsubsecfont}{\sffamily\color{muted}}
\renewcommand{\cftsubsecpagefont}{\sffamily\color{muted}}
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}
\renewcommand{\cfttoctitlefont}{\large\bfseries\sffamily\color{dark}}

% ============================================================
% 14. PANDOC COMPATIBILITY
% ============================================================
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\newcounter{none}

$if(highlighting-macros)$
$highlighting-macros$
$endif$

% ============================================================
% DOCUMENT BEGIN
% ============================================================
\begin{document}

% --- Title Page ---
\thispagestyle{empty}
\begin{center}
\vspace*{50mm}

{\fontsize{34pt}{40pt}\selectfont\bfseries\sffamily\color{dark}
CheeseStock}

\vspace{6mm}

{\fontsize{22pt}{28pt}\selectfont\sffamily\color{navy}
ANATOMY V8}

\vspace{8mm}

{\fontsize{13pt}{16pt}\selectfont\sffamily\color{muted}
이론적 정합성 흐름}

\vspace{20mm}

{\normalsize\sffamily\color{bodytext}
\begin{tabular}{rl}
\textbf{저자} & 이선호 \enspace\textperiodcentered\enspace 최민규 \\[4pt]
\textbf{소속} & CheeseStock 공동 창업자 \\[4pt]
\textbf{판본} & V8 \enspace|\enspace 2026년 4월 \\
\end{tabular}
}

\vspace{25mm}

{\normalsize\sffamily
\begin{tabular}{cl}
\textcolor{stageOneMarker}{\rule{8pt}{8pt}} & \textbf{제1장}\enspace 데이터와 API --- 원천 자료 \\[6pt]
\textcolor{stageTwoMarker}{\rule{8pt}{8pt}} & \textbf{제2장}\enspace 학술적 기반 --- 이론적 토대 \\[6pt]
\textcolor{stageThreeMarker}{\rule{8pt}{8pt}} & \textbf{제3장}\enspace 기술적 분석 --- 이론의 적용 \\[6pt]
\textcolor{stageFourMarker}{\rule{8pt}{8pt}} & \textbf{제4장}\enspace 차트 --- 시각적 변환 \\[6pt]
\textcolor{stageFiveMarker}{\rule{8pt}{8pt}} & \textbf{제5장}\enspace www.cheesestock.co.kr --- 최종 전달 \\
\end{tabular}
}

\end{center}
\clearpage

% --- Table of Contents ---
\thispagestyle{plain}
\setcounter{tocdepth}{2}
{%
  \setlength{\parskip}{0pt}%
  \small%
  \tableofcontents%
}
\vspace{6pt}

% --- Main Content (flows from TOC page) ---
$body$

\end{document}
